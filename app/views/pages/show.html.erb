<h1><%= @page.title %></h1>
<hr />
<p><%= @page.body %></p>
<hr />
<p><%= link_to "Wiki Home", wiki_path(@page.wiki.id) %> | 
<%= link_to "Edit Content", edit_wiki_page_path(wiki_id: @page.wiki.id, id: @page.id) %> | 
<%= link_to "Delete Page", [@page.wiki, @page], method: :delete, data: {confirm: 'Are you sure you want to delete this page?'} %> </p>